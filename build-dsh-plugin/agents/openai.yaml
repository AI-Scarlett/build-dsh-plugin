interface:
  display_name: "构建 DSH 插件"
  short_description: "生成 DSH 原生插件、工具卡片契约、商城候选与分级验收"
  default_prompt: "Use $build-dsh-plugin to build or audit a standard DSH plugin, define replay-safe Tool card contracts, prepare a DSH STORE-ready candidate when requested, and keep repository, marketplace, Profile, and evidence gates separate."
